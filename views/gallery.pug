extends index

mixin photo({path})
  img(src=path class="rounded-box")

block head 
  link(href="../stylesheets/style.css", rel="stylesheet")

block content
  h1(class="card-title") #{gallery.title}
  h2(class="card-body") #{gallery.description}
  each item in photos
    +photo(item)
    br