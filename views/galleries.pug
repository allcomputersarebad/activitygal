extends index

mixin gallery(g)
  .card-title
    a(href='/gallery/' + g.slug)
      = g.title
      .card-body
        p= g.description

block content
  div(class="hero-content")
    div(class="w-full")
      h1(class="mb-6 text-5xl font-bold") galleries
      p Please select a gallery to view
    nav
      each g in galleries
        div(class="card card-compact w-96 bg-base-100 shadow-xl")
          div(class="card-body")
            h2(class="card-title ")
              +gallery(g)
            div(class="cards-actions justify-end")
          g.description
        br